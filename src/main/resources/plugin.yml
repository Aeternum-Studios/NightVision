name: ${project.name}
version: ${project.version}
main: com.aeternum.nightvision.nightvision.ASNV
author: Aeternum
prefix: [ASNV]
commands:
  nv:
   description: Toggle NightVision
   aliases:
   - nightvision
permissions:
  asnv.use:
   description: Allows use of /nv
   default: op